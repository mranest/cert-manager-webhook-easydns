apiVersion: v1
kind: Service
metadata:
  name: {{ include "easydns-webhook.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "easydns-webhook.name" . }}
    chart: {{ include "easydns-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "easydns-webhook.name" . }}
    release: {{ .Release.Name }}
